#include <stdio.h>
#include <math.h>

int main() {
    float base, result;
    int exp;

    printf("Enter base and exponent: ");
    scanf("%f%d", &base, &exp);

    result = pow(base, exp);

    printf("Result = %.2f\n", result);

    return 0;
}
