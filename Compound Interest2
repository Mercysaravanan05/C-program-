#include <stdio.h>
#include <math.h>

int main() {
    float p, r, n, ci;

    printf("Enter Principal, Rate, Time: ");
    scanf("%f%f%f", &p, &r, &n);

    ci = p * pow((1 + r/100), n) - p;

    printf("Compound Interest = %.2f\n", ci);

    return 0;
}
